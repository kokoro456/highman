{
  "name": "highman-root",
  "private": true,
  "scripts": {
    "postinstall": "cd beauty-erp && npm install",
    "build": "cd beauty-erp && ./node_modules/.bin/prisma generate && cd apps/api && ./node_modules/.bin/nest build",
    "start": "cd beauty-erp/apps/api && node dist/main.js"
  },
  "engines": {
    "node": ">=20"
  }
}
